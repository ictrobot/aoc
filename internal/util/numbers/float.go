package numbers

// Float32MaxInt represents the maximum integer which can be represented using
// a float32 value without loss of precision
const Float32MaxInt = 1 << 24

// Float64MaxInt represents the maximum integer which can be represented using
// a float64 value without loss of precision
const Float64MaxInt = 1 << 53
